# npm configuration. Symlinked to ~/.npmrc by install.sh.

# Suppress the "N packages are looking for funding" notice after every
# install. It is advertising, it cannot be acted on programmatically, and it
# fires on each update.sh run. `npm fund` still reports on demand.
fund=false
